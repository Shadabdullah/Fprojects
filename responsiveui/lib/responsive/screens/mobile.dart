import 'package:flutter/material.dart';

class DesktopScaffole extends StatelessWidget {
  const DesktopScaffole({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blueAccent,
    );
  }
}
