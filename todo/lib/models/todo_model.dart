class Todo {
  String title;
  String byTime;
  DateTime dueDate;
  String category;
  bool isDone;
  String note;

  Todo(this.title, this.byTime, this.dueDate, this.category, this.isDone,
      this.note);
}
