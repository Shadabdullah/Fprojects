import 'package:flutter/material.dart';

class DpHomePage extends StatelessWidget {
  const DpHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Design Pattern"),
      ),
    );
  }
}
